# World-Increment Sweep + Hamming Swarm Snapshot

**Sweep timestamp:** 2026-05-05T08:17:40Z  
**Branch:** world-increment/sweep-2026-05-05-0817  
**DuckDB:** `packages/world-increment/ducklake/world-increments.duckdb`

---

## JOB 1: GitHub Social Graph Sweep

### Sources Queried

| Source | Type | Repos Found |
|--------|------|-------------|
| plurigrid | org | 100 |
| kubeflow | org | 47 |
| TeglonLabs | org | 4 |
| bmorphism | user | 101 |
| zubyul | user | 49 |
| migalkin | user (social) | 19 |
| DJedamski | user (social) | 6 |
| wasita | user (social) | 10 |
| kristinezheng | user (social) | 6 |
| M1shaaa | user (social) | 8 |
| AustinCStone | user (social) | 40 |

**Total repos snapshotted:** ~390 across 11 sources

### DuckDB Table: `world_increments`
GF(3) color chain applied per-increment:
- **trit=0 → ERGODIC #d3869b** (id%3==0)
- **trit=1 → PLUS #b8bb26** (id%3==1)  
- **trit=-1 → MINUS #cc241d** (id%3==2)

### DuckDB Table: `repo_snapshots`
Schema: `id, timestamp, increment_id, org_or_user, repo_name, full_name, language, stars, forks, open_issues, pushed_at, description`

### Notable Repos by Stars

**plurigrid:** `asi` (HTML, ★18), `ontology` (JS, ★7), `vcg-auction` (Rust, ★7)  
**kubeflow:** `kubeflow` (★15621), `pipelines` (★4132), `spark-operator` (★3123)  
**bmorphism:** `ocaml-mcp-sdk` (OCaml, ★60), `anti-bullshit-mcp-server` (JS, ★23)  
**migalkin:** `NodePiece` (Python, ★143), `StarE` (Python, ★89)  
**AustinCStone:** `TextGAN` (Python, ★92), `StereoVisionMRF` (Python, ★11)  

---

## JOB 2: Hamming Swarm Snapshot

### Aptos Mainnet Wallet Balances

All 28 addresses (alice, bob, A–Z) queried via Aptos fullnode mainnet API.  
**Result:** All accounts returned `resource_not_found` for `0x1::coin::CoinStore<0x1::aptos_coin::AptosCoin>`.  
Accounts either do not hold legacy APT CoinStore resources or have not been initialized on mainnet.  
**Balance recorded:** NULL for all 28 addresses.

| World | Address (prefix) | APT Balance |
|-------|-----------------|-------------|
| alice | 0xc793acdec12b4a... | NULL |
| bob | 0x0a3c00c58fdf90... | NULL |
| A–Z | (26 addresses) | NULL |

### Multisig Contract Probes

All 5 multisig contracts probed via `0x1::multisig_account::num_signatures_required` view function.

| Pair | Address | Sigs Required | Healthy |
|------|---------|---------------|---------|
| A-B | 0x0da4f428... | **2** | ✓ |
| A-G | 0xf56c4a1c... | **2** | ✓ |
| Y-Z | 0xd3ffe181... | **2** | ✓ |
| S-T | 0x3b1c3ae9... | **2** | ✓ |
| V-W | 0x40fad7b4... | **2** | ✓ |

**Status: All 5 multisig contracts healthy — 2-of-N threshold confirmed.**

### MNX Markets (testnet.mnx.fi)

Site is a Next.js SPA; all API paths (`/api/markets`, `/api/v1/markets`, `/api/tickers`) return HTML.  
**No machine-readable market data available.** Marked as unavailable in `mnx_snapshots` table (0 rows).

---

## DuckDB Table Summary

```
world_increments : GF(3)-colored increment log
repo_snapshots   : GitHub org/user repo metadata
aptos_snapshots  : Hamming swarm wallet balances (28 addresses)
multisig_probes  : Aptos multisig health checks (5 contracts)
mnx_snapshots    : MNX market data (unavailable this sweep)
```

## GF(3) Color Distribution

Each world-increment is assigned a trit via `id % 3`:
- **ERGODIC (#d3869b):** neutral/ergodic state
- **PLUS (#b8bb26):** positive increment  
- **MINUS (#cc241d):** negative/contraction

---

*Generated by world-increment-sweep + hamming-swarm-snapshot agent*  
*Model: claude-sonnet-4-6 | Date: 2026-05-05T08:17:40Z*
